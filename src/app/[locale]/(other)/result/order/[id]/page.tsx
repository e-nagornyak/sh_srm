import { OrderConfirmation } from "@/app/[locale]/(other)/result/order/[id]/_components/Page"

interface PageProps {}

export default function Page(props: PageProps) {
  return <OrderConfirmation />
}
